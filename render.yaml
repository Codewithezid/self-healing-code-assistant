services:
  - type: web
    name: langgraph-code-assistant-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python web_main.py
    autoDeploy: true
